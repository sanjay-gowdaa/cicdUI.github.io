import React from 'react'
import { Typography } from 'antd';

const { Title } = Typography;

const TransactionSection = () => {
    return (
        <Title level={2}>TransactionSection Section</Title>
    )
}

export default TransactionSection;